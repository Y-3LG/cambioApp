# Reglas propias del proyecto (vacío por ahora).
# Flutter, http, shared_preferences y connectivity_plus traen sus propias
# consumer-rules.pro empaquetadas; no usamos reflexión Java/Kotlin en este
# proyecto (la (de)serialización JSON es con dart:convert, no con Gson/etc),
# así que no hace falta agregar keep rules manuales.
